## Do While
```
fun main() {
    val brand = "Poopy B"
    var index = 0

    // Using do-while loop to iterate through the string
    do {
        println(brand[index])
        index++
    } while (index < brand.length)
}
```
